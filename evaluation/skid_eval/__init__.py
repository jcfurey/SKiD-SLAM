"""Reproducible evaluation for SKiD-SLAM.

Deliberately dependency-free apart from PyYAML, which the package already
requires for its parameter files: an evaluation that needs a scientific stack
installed is one that does not get run.
"""
